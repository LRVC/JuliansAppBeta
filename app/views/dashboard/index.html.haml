%h1 JULIAN'S SITE
%p= link_to 'Put some magic down', new_idea_path
-@ideas.each do |idea|
  %ul
    %li=link_to "#{idea.description}", idea_path(idea)
    =link_to 'Edit this idea', edit_idea_path(idea)
    =link_to 'Trash this idea', idea_path(idea), method: :delete
